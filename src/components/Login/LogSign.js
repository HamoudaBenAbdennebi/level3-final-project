import React from "react";
import Form from "./loginComponents/Form";
import FormLog from "./loginComponents/FormLog";

const LogSign = () => {
  return (
    <div>
      <Form />
      <FormLog />
    </div>
  );
};

export default LogSign;
